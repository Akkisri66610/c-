#include<iostream>
#include<string>// it is used for strlen , strcat etc functions are defined and declare
using namespace std;

// getline() and write() functions are used for strings insertion and printing
int main()
{
	// it is also based upon c i.e. strings are arrays of characters
	string line;
	cin.getline(line, );
	cout.write(line);
	return 0;
}
